CREATE TABLE sf (
	city TEXT,
	average_price DECIMAL(10,2),
	total_units INT,
	total_supply INT,
	entire_home_apt INT,
	hotel_room INT,
	private_room INT,
	shared_room INT
); 

CREATE TABLE nyc (
	city TEXT,
	average_price DECIMAL(10,2),
	total_units INT,
	total_supply INT,
	entire_home_apt INT,
	hotel_room INT,
	private_room INT,
	shared_room INT
); 

CREATE TABLE la (
	city TEXT,
	average_price DECIMAL(10,2),
	total_units INT,
	total_supply INT,
	entire_home_apt INT,
	hotel_room INT,
	private_room INT,
	shared_room INT
); 

CREATE TABLE hi (
	city TEXT,
	average_price DECIMAL(10,2),
	total_units INT,
	total_supply INT,
	entire_home_apt INT,
	hotel_room INT,
	private_room INT,
	shared_room INT
); 

CREATE TABLE cam (
	city TEXT,
	average_price DECIMAL(10,2),
	total_units INT,
	total_supply INT,
	entire_home_apt INT,
	private_room INT,
	shared_room INT
); 

CREATE TABLE bos (
	city TEXT,
	average_price DECIMAL(10,2),
	total_units INT,
	total_supply INT,
	entire_home_apt INT,
	hotel_room INT,
	private_room INT,
	shared_room INT
); 

CREATE TABLE bc (
	city TEXT,
	average_price DECIMAL(10,2),
	total_units INT,
	total_supply INT,
	entire_home_apt INT,
	hotel_room INT,
	private_room INT,
	shared_room INT
); 

CREATE TABLE chi (
	city TEXT,
	average_price DECIMAL(10,2),
	total_units INT,
	total_supply INT,
	entire_home_apt INT,
	hotel_room INT,
	private_room INT,
	shared_room INT
); 

CREATE TABLE colo (
	city TEXT,
	average_price DECIMAL(10,2),
	total_units INT,
	total_supply INT,
	entire_home_apt INT,
	hotel_room INT,
	private_room INT,
	shared_room INT
); 

CREATE TABLE den (
	city TEXT,
	average_price DECIMAL(10,2),
	total_units INT,
	total_supply INT,
	entire_home_apt INT,
	hotel_room INT,
	private_room INT,
	shared_room INT
); 